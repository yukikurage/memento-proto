// Test variance analysis
// This data type has T in contravariant position (function argument)
data Processor<T> : (callback : (x : T) => number) => Processor;

// Regular covariant data type (T appears in covariant position)
data Box<T> : (value : T) => Box;

// Test usage - the variance analysis should detect:
// - Processor<T> has T in contravariant position
// - Box<T> has T in covariant position
val string_processor : Processor := Processor((s : string) => 42);
val number_box : Box := Box(100);