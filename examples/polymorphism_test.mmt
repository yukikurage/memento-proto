// Test polymorphic functions
val identity<T> : (x : T) => T := (x : T) => x;

// Test polymorphic data type
data Some<T> : (value : T) => Option;
data None<T> : () => Option;

// Test usage of polymorphic function (with type inference)
val test_number : number := identity(42);
val test_string : string := identity("hello");

// Test main function that uses polymorphism
val main : () => number := () => test_number;
