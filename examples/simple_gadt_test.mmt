// Simple GADT test to debug bounds computation

data Box [
  MkBox<T> : (value : T) => Box<T>
];

val test : Box<number> := MkBox(42);

val result : number := switch (test) [
  (MkBox(x) : Box<number>) => x
];