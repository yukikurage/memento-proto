val is_even : number -> bool := (n ->
  if (n == 0) then true else (n - 1 |> is_odd)
);

val is_odd : number -> bool := (n ->
  if (n == 0) then false else (n - 1 |> is_even)
);

val test_even : bool := (4 |> is_even);
val test_odd : bool := (3 |> is_odd);
