// examples/simple_tuple.mmt

// Define tuple type
type Pair := [number, bool];

// Create a tuple instance
val tuple : Pair := [41, true];

val main : (input : number) => number
  := (args : number) => {
    let result : number := if(tuple[1]) { tuple[0] + 1 } else { tuple[0] };
    result
  };