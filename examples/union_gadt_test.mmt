// Test union types in GADT pattern matching

data Either [
  Left<T> : (value : T | bool) => Either<T | number>
];

val test_simple : Either<number> := Left(42);

val result : number | bool := switch (test_simple) [
  (Left(x) : Either<number>) => x
];